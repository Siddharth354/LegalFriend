# CLAUDE.md — Nyaya-Dost

> This file is a pointer, not the source of truth. **All operational rules — folder structure, stack, code style, logging, error handling, secrets, testing, commit discipline, deployment strategy — live in [`AGENTS.md`](AGENTS.md).** Read that file, not this one, before touching code.
>
> Why the pointer runs this direction here (rather than duplicating content in both, which is this repo's usual fresh-repo convention): the user wants one canonical file both Claude Code and Codex converge on, with CLAUDE.md staying thin so it never drifts out of sync with AGENTS.md by accident.

Claude Code reads this file first — go read [`AGENTS.md`](AGENTS.md) now.
